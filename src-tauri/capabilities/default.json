{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set. SYSTEM is local-first: no network, filesystem, or shell permissions are granted to the webview. All privileged work happens in audited Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
